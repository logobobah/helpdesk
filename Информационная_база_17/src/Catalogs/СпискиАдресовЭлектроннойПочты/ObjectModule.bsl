
Процедура ПередЗаписью(Отказ)
	
	ПредставлениеАдреса = "";
	
	Для Каждого Строка Из Адреса Цикл
		
		ПредставлениеСтроки = Строка.Представление;
		
		Если Не ЗначениеЗаполнено(ПредставлениеСтроки) Тогда
			ПредставлениеСтроки = Строка.Адрес;
		КонецЕсли;	
		
		Если ЗначениеЗаполнено(ПредставлениеАдреса) Тогда
			ПредставлениеАдреса = ПредставлениеАдреса + "; ";
		КонецЕсли;	
		
		ПредставлениеАдреса = ПредставлениеАдреса + ПредставлениеСтроки;
		
	КонецЦикла;	
	
КонецПроцедуры
